<?xml version='1.0' encoding='UTF-8'?>
<com.cloudbees.plugins.flow.BuildFlow plugin="build-flow-plugin@0.17">
  <actions/>
  <description></description>
  <keepDependencies>false</keepDependencies>
  <properties/>
  <scm class="hudson.scm.NullSCM"/>
  <canRoam>true</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenDownstreamBuilding>false</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>false</blockBuildWhenUpstreamBuilding>
  <triggers/>
  <concurrentBuild>false</concurrentBuild>
  <builders/>
  <publishers/>
  <buildWrappers/>
  <icon/>
  <dsl>
//out.println 'Initial unlock'
build(&quot;unlock&quot;)
//out.println 'Ruby-Gem-Packages'
//rm <%= File.dirname ENV['AUTOPROJ_CURRENT_ROOT'] %>/${release}/rock-osdeps.osdeps
ignore(FAILURE){
parallel (
    <% gems.each_with_index do |gem,i| %>
    {
    build(&quot;<%= gem%>&quot;, release: &quot;<%= flavor %>&quot;)}<%= gems.size == (i+1) ? "" : "," %>
    <% end %>
    )
}
//out.println 'Rock-Packages'
<% flow.each do |jobs| %>
ignore(FAILURE){
    parallel (
    <% jobs.each_with_index do |job,i| %>
    { build(&quot;<%= job %>&quot;, release: &quot;<%=  flavor %>&quot;)}<%= jobs.size == (i+1) ? "" : "," %>
    <% end %>
    )
}
<% end %>
</dsl>
  <buildNeedsWorkspace>false</buildNeedsWorkspace>
</com.cloudbees.plugins.flow.BuildFlow>
