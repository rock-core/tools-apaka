<?xml version='1.0' encoding='UTF-8'?>
<com.cloudbees.plugins.flow.BuildFlow plugin="build-flow-plugin@0.17">
  <actions/>
  <description></description>
  <keepDependencies>false</keepDependencies>
  <properties/>
  <scm class="hudson.scm.NullSCM"/>
  <canRoam>true</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenDownstreamBuilding>false</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>false</blockBuildWhenUpstreamBuilding>
  <triggers/>
  <concurrentBuild>false</concurrentBuild>
  <builders/>
  <publishers/>
  <buildWrappers/>
  <icon/>
  <dsl>
      //parallel (
    // ruby-gem-jobs will be scheduled in parallel as they don't depend on each other
    <% gems.each_with_index do |gem,i| %>
    //{ build(&quot;<%= gem%>&quot;, param1: &quot;test&quot;) }<%= gems.size == (i+1) ? "" : "," %>
    <% end %>
    //)

<% gems.each do |gem| %>
ignore(FAILURE) {
    build(&quot;<%= gem%>&quot;, param1: &quot;test&quot;)
}
<% end %>

<% flow.flatten.each do |job| %>
ignore(FAILURE) {
    build(&quot;<%= job %>&quot;, param1: &quot;test&quot;)
}
<% end %>
</dsl>
  <buildNeedsWorkspace>false</buildNeedsWorkspace>
</com.cloudbees.plugins.flow.BuildFlow>
