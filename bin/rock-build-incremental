#! /bin/bash -ex

source rock-build-common.sh

if test "x$1" = "xdev"; then
    USE_PRERELEASE=true
    shift
fi

BUILDCONF_GIT=$1
BUILDCONF_FILE=$2
MAIL_HEADER=$3
MAIL_ADDRESS=$4
MAIL_SMTP=$5

argument_checks
update

